# PHV Obsolete Idle Report Tool

Standalone Java/JasperReports CLI used by the .NET Web API to render the obsolete/idle PDF report from JSON input.

Build:

```bash
mvn -DskipTests package
```

Output:

```text
target/phv-obsolete-idle-report.jar
```